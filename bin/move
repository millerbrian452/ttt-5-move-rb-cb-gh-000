#!/usr/bin/env ruby

require_relative '../lib/move.rb'

puts "Welcome to Tic Tac Toe!"

board = Array.new(9, " ")

puts "Where would you like to go?"

input = gets.strip

input = input_to_index(input)

new_board = move(board, input, "X")

display_board(new_board)

# Code your CLI Here
